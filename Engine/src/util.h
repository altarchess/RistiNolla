#include <vector>
#include <string>

#define INVALID_POSITION std::cout << "Invalid position, position <x_size> <y_size> <turn> <position> " << std::endl;
#define INVALID_ARGUMENT std::cout << "Invalid arguments!" << std::endl;

std::vector<std::string> split(std::string);